const initialState = {
  login: true
};

const authReducer = function(state = initialState, action) {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default authReducer;
